---
title: gamegine.utils.NCIM.ComplexDimensions.acceleration
---

# gamegine.utils.NCIM.ComplexDimensions.acceleration

## Classes

### Acceleration

```python
class Acceleration
```

A class representing an acceleration complex measurement, composed of a spatial and temporal element in the form spatial^1 / temporal^2. Inherits from the :class:`ComplexMeasurement` class.

:param magnitude: The magnitude of the measurement.
:type magnitude: float
:param unit: The unit of the measurement.
:type unit: :class:`AccelerationUnit`
:param base_magnitude: The base magnitude of the measurement, used for internal creation. Defaults to None.
:type base_magnitude: float, optional

#### `__init__`

```python
def __init__(self, magnitude: float, unit: gamegine.utils.NCIM.ComplexDimensions.acceleration.AccelerationUnit, base_magnitude=None) -> None
```

Initialize self.  See help(type(self)) for accurate signature.

#### `convert_to`

```python
def convert_to(self, target_unit: gamegine.utils.NCIM.core.Unit) -> 'Measurement'
```

Convert this measurement to another unit and return a new Measurement object.

#### `to`

```python
def to(self, target_unit: gamegine.utils.NCIM.core.Unit) -> float
```

Convert this measurement to another unit and return the magnitude as a float.
This maintains backward compatibility with legacy NCIM.

### AccelerationUnit

```python
class AccelerationUnit
```

A class representing an acceleration complex unit, composed of a spatial and temporal element in the form spatial^1 / temporal^2, in addition to creating a :class:`Acceleration` of the same type when called. Inherits from the :class:`ComplexUnit` class.

:param spatial: The spatial unit of the acceleration.
:type spatial: :class:`SpatialUnit`
:param time: The temporal unit of the acceleration.
:type time: :class:`TemporalUnit`

#### `__init__`

```python
def __init__(self, spatial: gamegine.utils.NCIM.Dimensions.spatial.SpatialUnit, time: gamegine.utils.NCIM.Dimensions.temporal.TemporalUnit) -> None
```

Legacy shim for ComplexUnit(units_table).
Converts the dict \{Unit: power\} into dimensions/scale/symbol.

#### `from_base`

```python
def from_base(self, value: float) -> float
```

#### `to_base`

```python
def to_base(self, value: float) -> float
```

