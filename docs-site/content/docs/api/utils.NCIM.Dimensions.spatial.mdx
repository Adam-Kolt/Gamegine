---
title: gamegine.utils.NCIM.Dimensions.spatial
---

# gamegine.utils.NCIM.Dimensions.spatial

## Classes

### SpatialMeasurement

```python
class SpatialMeasurement
```

A class representing a spatial measurement.

#### `__init__`

```python
def __init__(self, magnitude: float, unit: gamegine.utils.NCIM.Dimensions.spatial.SpatialUnit, base_magnitude=None)
```

Initialize self.  See help(type(self)) for accurate signature.

#### `convert_to`

```python
def convert_to(self, target_unit: gamegine.utils.NCIM.core.Unit) -> 'Measurement'
```

Convert this measurement to another unit and return a new Measurement object.

#### `to`

```python
def to(self, target_unit: gamegine.utils.NCIM.core.Unit) -> float
```

Convert this measurement to another unit and return the magnitude as a float.
This maintains backward compatibility with legacy NCIM.

### SpatialUnit

```python
class SpatialUnit
```

A class representing a spatial unit of measurement.

#### `__init__`

```python
def __init__(self, scale: float = 1.0, display: str = '', shift: float = 0.0) -> None
```

Initialize self.  See help(type(self)) for accurate signature.

#### `from_base`

```python
def from_base(self, value: float) -> float
```

#### `to_base`

```python
def to_base(self, value: float) -> float
```

